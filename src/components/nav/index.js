import Logo from '../../img/logo.jpeg';

let Nav = `
      <nav>
        <p>nav bar</p>
        <img src="${Logo}" alt="Logotipo" class="logotipo">
        <a href="#/">Home</a>
        <a href="#/contato">Contato</a>
      </nav>
    `;
export default Nav;
